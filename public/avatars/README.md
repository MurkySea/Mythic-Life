# Companion avatars

| File | Use |
|------|-----|
| `{slug}.png` | Base headshot (profile default) |
| `{slug}-chibi.png` | Message list / party strip icon |

After adding a file, register the slug in `lib/staticAvatars.ts` (`STATIC_HEADSHOTS` / `STATIC_CHIBIS`).
